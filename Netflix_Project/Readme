# HTML folder contains the index.html file which will show the layout of the NETFLIX UI
# CSS folder contains the CSS script used in HTML file
# Images folder contains the assets required by the HTML sheet
